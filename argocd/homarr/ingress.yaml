apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homarr-ingress
  namespace: media
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  ingressClassName: traefik
  rules:
    - host: media.home
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: homarr-svc
                port:
                  name: http
