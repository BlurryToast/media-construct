apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
helmCharts:
  - name: traefik
    repo: https://traefik.github.io/charts
    version: "36.*"
    releaseName: traefik
    namespace: media
    valuesFile: values.yaml
